;; graphical configurations:
;; =========================

;; color theme
;; ===========
(setq nord-comment-brightness 20)
(setq nord-region-highlight "frost")
(load-theme 'nord t)

;; toggles
;; =======
(tool-bar-mode -1)
(scroll-bar-mode -1)
(menu-bar-mode -1)
(blink-cursor-mode -1)

;; misc.
;; =====

;; display columns
(column-number-mode)
